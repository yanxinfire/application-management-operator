apiVersion: v1
kind: Service
metadata:
  name: my-test-ing
  namespace: my-test
  labels:
    app: my-test-ing
    owner: xin_yan
spec:
  selector:
    app: my-test-ing
  ports:
    - protocol: TCP
      port: 80
      targetPort: "http"
  type: ClusterIP