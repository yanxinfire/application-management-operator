apiVersion: v1
kind: Service
metadata:
  name: my-test-np
spec:
  selector:
    app: my-test-np
  ports:
    - name: http
      protocol: TCP
      port: 80
      nodePort: 30006
      targetPort: 80
  type: NodePort