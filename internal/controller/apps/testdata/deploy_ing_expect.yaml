apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-test-ing
  labels:
    app: my-test-ing
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-test-ing
  template:
    metadata:
      name: my-test-ing
      labels:
        app: my-test-ing
    spec:
      containers:
        - name: my-test-ing
          image: nginx
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
              protocol: TCP
      restartPolicy: Always